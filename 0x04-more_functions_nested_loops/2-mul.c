#include "main.h"

/**
 * mul - Multiplies two digits
 * @a: First digit to multiply
 * @b: Second digit to multiply with first digit
 * Return: Result of multiplication between x param and y param
 */

int mul(int a, int b)

{
	return (a * b);

}
