#include "main.h"
void _puts(char *str)
#include <stdio.h>
/**
 * main - starting point
 * Return: 0 always when successful
 */

int main(void)

{
	puts("\"Programming is like building a multiligual pussle");

	return (0);
}
